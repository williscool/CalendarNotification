<resources>

    <!-- Base application theme with Material 3 DayNight for automatic dark mode -->
    <style name="AppTheme" parent="Theme.Material3.DayNight">
        <!-- Primary brand colors -->
        <item name="colorPrimary">@color/primary</item>
        <item name="colorOnPrimary">@color/icons</item>
        <item name="colorPrimaryContainer">@color/primary_light</item>
        <item name="colorOnPrimaryContainer">@color/primary_dark</item>
        
        <!-- Secondary/accent colors -->
        <item name="colorSecondary">@color/accent</item>
        <item name="colorOnSecondary">@color/icons</item>
        
        <!-- Surface colors -->
        <item name="colorSurface">@color/cardview_light_background</item>
        <item name="colorOnSurface">@color/primary_text</item>
        <item name="colorSurfaceVariant">@color/background</item>
        <item name="colorOnSurfaceVariant">@color/secondary_text</item>
        
        <!-- Background -->
        <item name="android:colorBackground">@color/background</item>
        <item name="android:windowBackground">@color/background</item>
        
        <!-- Preference list styling -->
        <item name="android:listViewStyle">@style/AppListView</item>
        
        <!-- Bottom sheet dialog theme for proper theming -->
        <item name="bottomSheetDialogTheme">@style/AppTheme.BottomSheetDialog</item>
    </style>
    
    <style name="AppListView" parent="android:Widget.ListView">
        <item name="android:background">@color/background</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.Material3.Dark.ActionBar"></style>

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.Material3.Light"></style>

    <!-- Bottom sheet dialog theme - uses surface color for consistency -->
    <style name="AppTheme.BottomSheetDialog" parent="ThemeOverlay.Material3.BottomSheetDialog">
        <item name="bottomSheetStyle">@style/AppTheme.BottomSheet</item>
    </style>

    <style name="AppTheme.BottomSheet" parent="Widget.Material3.BottomSheet.Modal">
        <item name="backgroundTint">@color/cardview_light_background</item>
    </style>

    <style name="PickerDialogTheme" parent="AppTheme.NoActionBar">
        <item name="android:textSize">20sp</item>
        <item name="android:labelTextSize">20dp</item>
    </style>

    <style name="ToolbarTitle" parent="@style/TextAppearance.Material3.TitleLarge">
        <item name="android:textSize">20sp</item>
    </style>

</resources>
